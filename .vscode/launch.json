{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/vscode-markit"],
      "preLaunchTask": "build-extension",
      "outFiles": ["${workspaceFolder}/vscode-markit/dist/**/*.js"]
    }
  ]
}
