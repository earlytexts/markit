{
  "name": "@earlytexts/markit",
  "version": "3.4.2",
  "description": "Markit is a lightweight textual markup language similar to Markdown, but designed for use in textual preservation projects, as a more human-readable alternative to TEI XML. It compiles to JSON for representing document structure and metadata, while the text content itself can further be compiled to either plain text or HTML.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/earlytexts/markit"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "workspaces": [
    "vscode-markit"
  ],
  "scripts": {
    "bump": "npm version --workspaces --include-workspace-root --no-git-tag-version",
    "build": "node esbuild.js && tsc -p tsconfig.build.json",
    "build:vscode": "cd vscode-markit && npm run build",
    "build:all": "npm run build && npm run build:vscode",
    "build:watch": "node esbuild.js --watch",
    "test": "vitest run && npm run check && npm run format:check",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage --coverage.reporter=text",
    "check": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepublishOnly": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^25.5.1",
    "@vitest/coverage-v8": "^4.0.18",
    "esbuild": "^0.27.3",
    "prettier": "^3.4.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
